"""Search infrastructure tests temporarily disabled."""

import pytest

pytest.skip(
    "Search infrastructure adapters removed during migration; tests pending rewrite.",
    allow_module_level=True,
)
